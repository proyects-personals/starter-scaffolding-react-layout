import type { AppTheme } from "@domain";

/**
 * @file light.theme
 * @description Definición del tema claro de la aplicación.
 *              Contiene la paleta de colores base utilizada cuando el
 *              sistema o el usuario selecciona el modo claro.
 *
 * @author Steveen Cues
 * @version 1.0.0
 */

export const lightTheme: AppTheme = {
  name: "light",
  colors: {
    background: "#ffffff",
    text: "#0f172a",
    primary: "#2563eb",
  },
};
