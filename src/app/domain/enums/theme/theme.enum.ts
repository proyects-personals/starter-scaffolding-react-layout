/**
 * Enumeración de Temas Visuales (ThemeEnum).
 * * @description
 * Define los estados permitidos para el motor de estilos de la aplicación.
 * El uso de un Enum asegura que cualquier cambio en el nombre de los temas
 * se refleje automáticamente en toda la lógica de negocio (Providers, Hooks y Components).
 * * @enum {string}
 */
export enum ThemeEnum {
  LIGHT = "light",
  DARK = "dark",
}
